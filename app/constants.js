const createTimeFormat = 'YYYY-MM-DD HH:mm:ss';
const UPLOADS_PATH = `${__dirname}/assets/uploads/`;
const ASSETS_PATH = `${__dirname}/assets/`;

module.exports = {
    createTimeFormat,
    UPLOADS_PATH,
    ASSETS_PATH,
};